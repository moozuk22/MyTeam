const fs = require('fs');
const path = 'd:/Documents/GitHub/MyTeam/src/app/admin/members/page.client.tsx';
let src = fs.readFileSync(path, 'utf8');

// ── 1. Add guide button before the closing </div> of amp-buttons-grid ────────
const btnAnchor = '<span>Съобщение</span>\r\n            </button>\r\n          )}\r\n        </div>\r\n        {(isClubPushSupported';
const btnInsert = `<span>Съобщение</span>\r\n            </button>\r\n          )}\r\n          <button\r\n            className="amp-guide-btn amp-btn--compact"\r\n            type="button"\r\n            onClick={() => { setGuideStep(0); setGuideOpen(true); }}\r\n          >\r\n            <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round" aria-hidden="true">\r\n              <circle cx="12" cy="12" r="10" />\r\n              <path d="M9.09 9a3 3 0 0 1 5.83 1c0 2-3 3-3 3" />\r\n              <circle cx="12" cy="17" r="0.5" fill="currentColor" />\r\n            </svg>\r\n            <span>Наръчник</span>\r\n          </button>\r\n        </div>\r\n        {(isClubPushSupported`;

if (!src.includes(btnAnchor)) { console.error('button anchor not found'); process.exit(1); }
src = src.replace(btnAnchor, btnInsert);
console.log('button inserted');

// ── 2. Add guide modal before </main> ────────────────────────────────────────
// Find </main> near the end
const mainIdx = src.lastIndexOf('    </main>\r\n  );\r\n}\r\n\r\nexport default function AdminMembersPage()');
if (mainIdx === -1) { console.error('modal anchor not found'); process.exit(1); }

const modalCode = `      {guideOpen && (\r\n        <div className="amp-overlay" onClick={() => setGuideOpen(false)}>\r\n          <div className="amp-modal amp-modal--guide" onClick={(e) => e.stopPropagation()}>\r\n            <div className="amp-modal-tint" aria-hidden="true" />\r\n            <div className="amp-guide-progress-bar" aria-hidden="true">\r\n              <div\r\n                className="amp-guide-progress-fill"\r\n                style={{ width: \`\${((guideStep + 1) / guideSteps.length) * 100}%\` }}\r\n              />\r\n            </div>\r\n            <h2 className="amp-modal-title" style={{ marginBottom: "20px" }}>\r\n              <span className="amp-modal-title-gradient">Наръчник</span>\r\n              <div className="amp-modal-title-actions">\r\n                <span className="amp-guide-counter">{guideStep + 1} / {guideSteps.length}</span>\r\n                <button className="amp-modal-close" onClick={() => setGuideOpen(false)} aria-label="Затвори">\r\n                  <XIcon />\r\n                </button>\r\n              </div>\r\n            </h2>\r\n            <div className="amp-guide-step-body">\r\n              <div className="amp-guide-step-num" aria-hidden="true">{guideStep + 1}</div>\r\n              <div>\r\n                <h3 className="amp-guide-step-title">{guideSteps[guideStep]?.title}</h3>\r\n                <p className="amp-guide-step-desc">{guideSteps[guideStep]?.description}</p>\r\n              </div>\r\n            </div>\r\n            {guideStep === guideSteps.length - 1 && (\r\n              <div className="amp-guide-toc">\r\n                <p className="amp-guide-toc-label">Върни се към:</p>\r\n                <div className="amp-guide-toc-grid">\r\n                  {guideSteps.map((step, idx) => (\r\n                    <button\r\n                      key={step.id}\r\n                      className={\`amp-guide-toc-item\${idx === guideStep ? " is-active" : ""}\`}\r\n                      type="button"\r\n                      onClick={() => setGuideStep(idx)}\r\n                    >\r\n                      <span className="amp-guide-toc-num">{idx + 1}</span>\r\n                      <span className="amp-guide-toc-name">{step.title}</span>\r\n                    </button>\r\n                  ))}\r\n                </div>\r\n              </div>\r\n            )}\r\n            <div className="amp-guide-nav">\r\n              <button\r\n                className="amp-btn amp-btn--ghost amp-btn--compact"\r\n                type="button"\r\n                onClick={() => setGuideStep((s) => Math.max(0, s - 1))}\r\n                disabled={guideStep === 0}\r\n              >\r\n                ← Назад\r\n              </button>\r\n              {guideStep === guideSteps.length - 1 ? (\r\n                <button\r\n                  className="amp-btn amp-btn--primary amp-btn--compact"\r\n                  type="button"\r\n                  onClick={() => setGuideOpen(false)}\r\n                >\r\n                  Готово ✓\r\n                </button>\r\n              ) : (\r\n                <button\r\n                  className="amp-btn amp-btn--primary amp-btn--compact"\r\n                  type="button"\r\n                  onClick={() => setGuideStep((s) => s + 1)}\r\n                >\r\n                  Напред →\r\n                </button>\r\n              )}\r\n            </div>\r\n          </div>\r\n        </div>\r\n      )}\r\n    </main>\r\n  );\r\n}\r\n\r\nexport default function AdminMembersPage()`;

src = src.slice(0, mainIdx) + modalCode + src.slice(mainIdx + '    </main>\r\n  );\r\n}\r\n\r\nexport default function AdminMembersPage()'.length);
console.log('modal inserted');

fs.writeFileSync(path, src, 'utf8');
console.log('OK - button + modal injected');
